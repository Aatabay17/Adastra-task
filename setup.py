from setuptools import setup

setup(
    name='movie_dataset_analyzer',
    version='1.0.0',
    description='Python program to analyze movie dataset',
    author='Alim Atabay',
    packages=[''],
    install_requires=[
        'pandas>=1.0.0'
    ],
)